
#   Reverse the String inside the Parenthesis

####    Function    `reverseTheStringInParens()`

Reverse the string inside the Parenthesis.

input `foo(bar)blim`


####    Output  String

output `foorabblim`